# Calculadora Modular Visual

Visualizador interativo de operações em aritmética modular (Z_n), com animações no círculo modular e grafo de conexões.

## Como rodar

```bash
npm install
npm run dev
```

O servidor abre em `http://localhost:3000`.
